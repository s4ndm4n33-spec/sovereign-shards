# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.venv/

# Models (too large for git)
models/*.gguf
models/*.bin

# Binaries / embedded runtime — USB-only, never tracked.
# README "Repository Layout" keeps these at root; they must not enter git
# (a stray `git add -A` would otherwise commit ~140MB of llama.cpp binaries).
model-server/
python/
get-pip.py

# Logs (local only)
logs/
*.log
.j_chain.json

# Environment
.env

# OS
.DS_Store
Thumbs.db
Desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Temp
*.tmp
*.bak
scratch/

# GitHub agent output
j_response.md
